spring:
  datasource:
    url: jdbc:mysql://localhost:3306/propbuddy_dev_db
    username: root
    password: root

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
    show-sql: true

